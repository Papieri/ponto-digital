# Postgres local em Docker — ver "Comece por aqui" no CLAUDE.md
DATABASE_URL=postgres://postgres:devlocal@localhost:5432/ponto

# Marco 1 não usa storage nem sessão; ficam aqui porque a especificação já os prevê.
UPLOAD_DIR=./uploads
SESSION_SECRET=qualquer-coisa-longa-em-dev
